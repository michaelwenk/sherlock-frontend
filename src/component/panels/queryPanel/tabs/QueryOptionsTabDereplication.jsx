/** @jsxImportSource @emotion/react */

import Tolerances from '../../../elements/Tolerances';

function QueryTabDereplication({ tolerance, onChangeTolerance }) {
  return (
    <Tolerances tolerance={tolerance} onChangeTolerance={onChangeTolerance} />
  );
}

export default QueryTabDereplication;
